name: cards
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  auto_animated: ^3.0.0
  flutter_staggered_grid_view: ^0.4.0
  get: ^4.0.0-nullsafety.2
  get_storage: ^2.0.1
  shared_preferences: ^2.0.6
  provider: ^5.0.0
  line_icons: ^2.0.1
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/google_fonts/
    - assets/data/Simple.json

